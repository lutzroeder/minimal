runtime=node                                    # go, node or python
server=x.x.x.x                                  # SSH server address
user=johnsmith                                  # SSH server user name
site=Minimal                                    # Website root /var/www/${site} and systemd service name 
domain=                                         # xxxx.com domain name for NGNIX reverse proxy and Let's Encryt registration
email=                                          # Email address for Let's Encrypt registration